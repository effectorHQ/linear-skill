name: Skill Lint

on:
  push:
    branches: [main]
    paths:
      - 'SKILL.md'
  pull_request:
    paths:
      - 'SKILL.md'

jobs:
  lint:
    uses: effectorHQ/.github/.github/workflows/reusable-skill-lint.yml@main
    with:
      fail-on-warnings: true
